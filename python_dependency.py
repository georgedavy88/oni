#!/usr/bin/env python

from setuptools import setup

setup(name='ONI-python',
      description='ONI python dependencies',
      install_requires=['lxml','scrapy','sh','wheel','watchdog','pika'],
     )
